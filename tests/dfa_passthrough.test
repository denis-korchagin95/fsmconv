@test("DFA input passes through unchanged")
@given("file")
#initial q0;
#final q3;

q0 -> q1 by 'a';
q1 -> q2 by 'b';
q2 -> q3 by 'c';

@whenRun("./bin/fsmconv")
@expectOutput("stdout")
#initial s0;
#final s3;

s0 to s1 by 'a';
s1 to s2 by 'b';
s2 to s3 by 'c';

@endtest
